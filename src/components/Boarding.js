import React from "react";
import {StyledDiv} from "../styles/style";
const Boarding = () => {
    return (
        <StyledDiv>
          <h2>Boarding </h2>
       <p>All boarding is done in the house rather than kennels
          so it is a very relaxed and natural homely environment </p>
        </StyledDiv>
      );
    };
    
    export default Boarding ;

